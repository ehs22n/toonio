**toonio** یک پکیج برای ارسال داده‌ها در فرمت **Toon** است، `که چندین برابر سریع‌تر از JSON است`.

این کتابخانه همچنین از ادغام با **Django** پشتیبانی می‌کند و با **FastAPI** نیز سازگار است.

این کتابخانه دو قابلیت مهم برای **Django** ارائه می‌دهد. به عنوان مثال، می‌توانید نماهای `پویا` ایجاد کنید که بسته به درخواست، `JSON` یا `Toon` بازگردانند.

# toonio — راهنمای ادغام با Django

## استفاده

### بازگرداندن **Toon** برای همه درخواست‌ها

```python
REST_FRAMEWORK = {
    'DEFAULT_RENDERER_CLASSES': [
        'toonio.django.render.ToonRenderer',
    ],
    'DEFAULT_PARSER_CLASSES': [
        'toonio.django.parser.ToonParser',
    ],
}
```

## فعال‌سازی حالت پویا (JSON یا Toon بر اساس هدرها)

```python
REST_FRAMEWORK = {
    'DEFAULT_RENDERER_CLASSES': [
         'toonio.django.render.DynamicRenderer',
    ],
    'DEFAULT_PARSER_CLASSES': [
        'toonio.django.parser.DynamicParser',
    ],
}
```

# نمونه هدرها

**ارسال JSON → دریافت Toon**:
```bash
Content-Type: application/json
Accept: application/x-toon
```

**ارسال Toon → دریافت Toon**:

```bash
Content-Type: application/x-toon
Accept: application/x-toon
```

**ارسال Toon → دریافت JSON**:

```bash
Content-Type: application/x-toon
Accept: application/json
```

---

## تنظیمات برای هر نما

### نمای پویا

```python
from toonio.django.parser import DynamicParser
from toonio.django.render import DynamicRenderer

class Index(APIView):
    parser_classes = [DynamicParser]
    renderer_classes = [DynamicRenderer]

    def get(self, request, *args, **kwargs):
        return Response({"message": "Hello Toon"})
```

### نمای فقط Toon

```python
from toonio.django.parser import ToonParser
from toonio.django.render import ToonRenderer

class Index(APIView):
    parser_classes = [ToonParser]
    renderer_classes = [ToonRenderer]

    def get(self, request, *args, **kwargs):
        return Response({"message": "Hello Toon"})
```

---

# استفاده در FastAPI

```python
from fastapi import FastAPI
from toonio.response import Response
from toonio.status import HTTP_200_OK

app = FastAPI()

@app.get("/")
def hello_toon():
    return Response({"Hello": "Toon"}, status_code=HTTP_200_OK)  # بازگرداندن پاسخ Toon
```

---

# مشارکت در پروژه

ما از تمامی مشارکت‌ها در Toonio استقبال می‌کنیم!  
چه ایده‌ای برای بهبود داشته باشید، باگی پیدا کرده باشید یا بخواهید ویژگی جدیدی اضافه کنید، ما خوشحال می‌شویم که کمک شما را داشته باشیم.

### نحوه مشارکت

- برای تغییرات کوچک، می‌توانید مستقیماً Pull Request باز کنید.  
- برای ویژگی‌های بزرگ یا تغییرات مهم، ابتدا Issue ایجاد کنید تا رویکرد را با هم بررسی کنیم.  
- مطمئن شوید که کد تمیز، خوانا و دارای تست‌های لازم باشد.

🐞 **گزارش مشکلات**

اگر با مشکلی مواجه شدید:

1. ابتدا بررسی کنید که آیا قبلاً Issue مشابهی ایجاد شده است یا خیر.  
2. در صورت عدم وجود، یک Issue جدید ایجاد کنید و موارد زیر را ارائه دهید:
   - توضیح واضح مشکل  
   - مراحل بازتولید  
   - نسخه‌های Python/فریمورک/سیستم‌عامل  
   - هرگونه لاگ یا پیام خطای مرتبط  

ما تلاش می‌کنیم تا سریعاً پاسخ دهیم و مشکلات را برطرف کنیم.

---

# مجوز Toonio (مجوز آزاد مبتنی بر MIT)

Copyright (c) 2025 Toonio Developers

بدین وسیله اجازه داده می‌شود، بدون هیچ هزینه‌ای، به هر شخصی که یک نسخه از این نرم‌افزار و مستندات آن (به اختصار "نرم‌افزار") را دریافت کرده است،  
بدون هیچ محدودیتی، از جمله حق استفاده، کپی، تغییر، ادغام، انتشار، توزیع، اعطای زیرمجوز و/یا فروش نسخه‌های نرم‌افزار استفاده کند،  
و به افرادی که نرم‌افزار برای آن‌ها فراهم شده اجازه دهد این کار را انجام دهند، مشروط بر اینکه شرایط زیر رعایت شود:

اطلاعیه حق نشر فوق و این مجوز باید در تمام نسخه‌ها یا بخش‌های مهم نرم‌افزار گنجانده شود.

این نرم‌افزار به صورت "همان‌طور که هست" ارائه می‌شود، بدون هیچ گونه ضمانتی، چه صریح و چه ضمنی،  
از جمله اما نه محدود به ضمانت‌های تجاری، مناسب بودن برای هدف خاص و عدم نقض حقوق.  
در هیچ شرایطی نویسندگان یا دارندگان حقوق نمی‌توانند مسئول هیچ گونه ادعا، خسارت یا مسئولیت دیگری باشند، چه در قرارداد، چه در تخلف یا سایر موارد، که از یا در ارتباط با نرم‌افزار ناشی شود.

این پروژه کاملاً رایگان و متن‌باز است.  
**هیچ هزینه، حق امتیاز یا نیاز به اشتراک وجود ندارد**،  
و شما می‌توانید از آن در پروژه‌های شخصی، آموزشی و تجاری استفاده کنید.